spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

auth:
  jwt:
    secret: ${JWT_SECRET}

notification:
  internal:
    secret: ${NOTIFICATION_INTERNAL_SECRET}
