server:
  port: 8087

spring:
  application:
    name: audit-service
  datasource:
    url: jdbc:mysql://localhost:3306/hcmcyu_audit_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

audit:
  internal:
    secret: ${AUDIT_INTERNAL_SECRET:dev-audit-internal-secret}

management:
  endpoints:
    web:
      exposure:
        include: health,info
